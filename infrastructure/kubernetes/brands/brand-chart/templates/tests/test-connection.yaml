apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brand-chart.fullname" . }}-test-connection"
  labels:
    {{- include "brand-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brand-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
